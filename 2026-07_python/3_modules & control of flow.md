# 2026-07-22

## 오늘 공부한 내용

-math 내장 모듈
dot 연산
from 절 사용- 필요한 것만 콕 집어서 가져오는 방식
import 모듈- 모듈 이름이 짧고, 어디서 온 함수인지 드러내고 싶을 때
from 모듈 import 대상 -특정 함수/변수를 자주 반복호출할 때, 모듈 경로가 깊을 때
'as'키워드 (alias)별칭을 부여
* <- 사용하지 않는 편이 좋다 
**패키지(Package)**
패키지는 이미 누군가 잘 만들어둔 코드 쿠러미
가구를 *조립할 때 공구 세트가 있으면 편한* 것처럼, 파이썬도 사용자가 모든 기능을
다 만들기 어렵습니다.  
패키지 사용 목적 
모듈들의 이름공간을 구분하여 충돌을 방지/ 모듈들을 관리
**라이브러리**
라이브러리>패키지>모듈
도서관>책장>책 1권
파이썬 표준 라이브러리 (PSL)
파이썬 언어와 함꼐 제공되는 ??
pip - 외부 패키지들을 설치하도록 도움
패키지 설치 
최신 버전// 특정 버전/ 최소 버전을 명시하여 설치할 수 있음
$ pip install SomePackage
$ pip install SomePackage ==?
???

requests 외부 패키지 설치 및 사용 예시
requests 패키지 
파이썬에서 웹에 요청을 보내고 응답을 받는 걸 아주 쉽게 만들어주는 외부 패키지

**제어문**
constrol statement
코드의 실행 흐름을 제어하는 데 사용되는 구문
조건에 따라 코드 블록을 실행하거나 반복적으로 코드를 실행
*조건문* 
if, elif, else
*반복문*
for 반복
*while 반복*
주어진 조건식을 평가하여 해당 조건이 참인 경우에만 코드 블록을 실행하거나 건너뜀
조건문의 기본 구조
if -조건문의 기본 형태 if문에 작성된 조건을 만족할 때 내부 코드 실행
작성되는 조건은 표현식으로 작성
elif -이전의  조건을 만족하지 못하고 추가로 다른 조건이 필요할 때 사용
여러 개 사용 가능, 순서대로 조건을 봄
else 모든 조건을 만족하지 않으면 실행할 코드
*조건문의 순서*
조건식을 동시에 검사하는 게 아니라 순차적으로 비교
조건식의 순서에 따라 원하는 결과가 나오지 않을 수 있음
*독립 if 여러 개 vs elif*
같아 보이나 결과가 완전히 다릅니다
*중첩 조건문*
조건문 내부에 또 다른 조건문 작성 가능
컴퓨터에게 "동시에 검사" ==> 모호함이 생김
시스템은 반드시 하나를 선택
"위에서부터, 첫번째 true에서 종료"라는 규칙을 만든 건 이 충돌을 결정론적으로
해소하기 위함
실행 순서를 안 정했다고? =>같은 코드가 실행할 때마다 다른 결과 => 재현이 불가능한 프로그램
기술적인 이유: 컴퓨터 계산 모델 CPU 
CPU가 명령어를 실행하는 방식이 본질적으로 한 번에 하나씩 순서대로 처리함..
근데 동시 검사도 존재함
-디지털 논리 회로
**반복문**
주어진 코드 블록을 반복해서 실행하는 구문
반복문의 종류
*for문* 
반복 가능(iterable)한 객체의 요소들을 반복하는데 주로 사용
주로 반복 가능한 객체 요소의 개수만큼 반복
특징: 반복 횟수가 정해져 있음
*while문*
while 조건이 참(True)인 동안 반복 == False가 될 때까지 반복 (수렴)
반복 횟수가 정해지지 않은 경우 주로 사용 
**for 반복문** 
반복 가능한 객체의 요소들을 반복하는데 사용
반복 가능한 객체의 요소 개수만큼 반복이 수행됨
for문 작동 원리
리스트 내 첫 항목이 반복 변수(item)에 할당되고 코드블록이 실행
다음으로 반복 변수에 리스트의 2번째 항목이 할당되고 코드블록이 다시 실행
...마지막으로 반복 변수에 리스트의 마지막 요소가 할당되고 코드블록이 실행
더 이상 반복 변수에 할당할 값이 없으면 반복 종료
반복 변수는 단수형으로 사용 권장
문자열도 시퀀스 
range 순회 
range 함수(연속된 정수 범위를 생성해주는 파이썬 내장 함수)
딕셔너리 순회
dict 자료형은 비시퀀스 자료형으로 반복 순서가 보장되지 않음을 주의
*인덱스로 리스트 순회*
리스트의 요소가 아닌 인덱스로 접근하여 해당 요소들을 변경하기
인덱스를 사용하면 리스트의 원하는 위치에 있는 값을 읽거나 변경할 수 있음
*중첩 리스트 순회*
안쪽 리스트 요소에 접근하려면 바깥 리스트를 순회하면서 중첩 반복을 사용해 각 안쪽 반복을 순회
**2차원 리스트 순회 - IM**
*while* 
주어진 조건식이 참(True)인 동안 코드를 반복해서 실행
==
조건식이 거짓(False)이 될 때까지 반복해서 실행
무한루프 주의 
while문의 특징
*반드시 종료 조건이 필요*
**반복 제어**
for문과 while은 매 반복마다 본문 내 모든 코드를 실행하지만 때때로 일부만 실행하는 것이 필요할 때가 있음
*break*
해당 키워드를 만나게 되면 남은 코드를 무시하고 반복 즉시 종료
반복을 끝내야 할 명확한 조건이 있을 때 사용
*continue*
해당 키워드를 만나게 되면 다음 코드는 무시하고 다음 반복을 수행
반복 제어문은 반드시 반복문 내에서 사용
*빈 코드 블록 키워드*
실습에서 pass가 써져 있는 이유
pass - '아무 동작도 하지 않음'을 명시적으로 나타내는 키워드
반복 제어가 아닌 코드의 틀을 유지하거나 나중에 내용을 채우기 위한 용도
코드를 비워두면 오류가 발생하기 때문
반복문 뿐만 아니라 함수, 조건문에서도 사용 가능
**유용한 내장 함수 map**
반복 가능한 데이터구조의 모든 요소ㅇ에 function을 적용하고, 그 결과 값들을 map object로 묶어서 반환
map이 쓸 수 있도록 함수는 ()호출 안하고 이름만,
만약 인자가 들어가면 넣기
**for-else**
for루프가 break를 만나 중단되지 않고, 끝까지 정상적으로 완료되었을 때만 else 블록이 실행 
**enumerate**
iterable객체의 각 요소에 대해 인덱스와 값을 함께 반환하는 내장함수
enumerate 함수 활용
enumerate의 index정보를 이용해 넘버링으로 사용
**zip(*iterables)** 
zip 함수는 여러 개의 반복 가능한 데이터 구조를 묶어서, 같은 위치에 있는 값들을 하나의 튜플로 만든 뒤 그것들을 모아 zip object로 반환하는 함수


## 새롭게 배운 점

-반복/ 조건 -> 코딩의 전부. 
-코드업 100제
-반복문을 최소 1~2바퀴 정도까지는 직접 변수의 변화를 손으로 체크해보는 습관
-새로운 변수를 만들 수 있는 능력
온라인에서 자료를 참고할 때 주의사항
- 블로그 글은 우선순위가 가장 낮음
- 공식문제를 메인으로 보는것은 비추
- 공식문서는 다 해결한 뒤에 최종 정리할때 보강 및 확인하는 용도 
- AI를 1순위로 두고 힌트를 줄 수 있도록 세팅하기 
## 실습 코드

```bash
```

## 문제와 해결 방법

-

## 참고 자료

-

## 내일 공부할 내용

-