# 2026-07-24

## 오늘 공부한 내용

친구 번호를 검색하기 쉬운 구조로 바꿔 볼까요?
키-값 구조/ 인덱스 대신 키 사용/ 자료구조 선택

-**비시퀀스 데이터 구조**
시퀀스는 뭐 하나 찾으려면 처음부터 싹 돌려야됨 비시퀀스는 이름표를 바로 위치로 바꿔 한 번에 찾을 수 있음 = 데이터 크기가 커질수록 유리
*딕셔너리*
내부적으로 해시 테이블을 사용하여 키-값 쌍을 관리
키(Key)는 hashable한 고유 값이어야 하지만, 값(Value)은 중복 가능, 어떤 자료형도 저장 가능 
*딕셔너리 메서드*
D.get(key[,default])
키에 연결된 값을 반환
키가 없으면 None 혹은 기본 값을 반환
.keys()
딕셔너리 키를 모은 객체를 반환
.values()
딕셔너리 값을 모은 객체를 반환
.items()
딕셔너리 키/값 쌍을 모은 객체를 반환
.setdefault(key[,defaut])
키에 연결된 값을 반환
키가 없다면 default와 연결한 키를 딕셔너리에 추가하고 default를 반환
.update()
other가 제공하는 키/값 쌍으로 딕셔너리를 갱신하고 기존 키는 덮어씀 
.pop(key[,default])
키를 제거하고 연결됐던 값을 반환(없으면 에러나 default를 반환)
.clear()
딕셔너리의 모든 키/값 쌍을 제거
**딕셔너리의 확장: defaultdict**
파이썬 내장 모듈 collections에서 제공하는 딕셔너리의 확장판
defaultdict 구문 및 활용
defaultdict(자료형)
자주 쓰는 패턴
1. 숫자 세기: defaultdict(int)
   키가 없으면 0으로 초기화
2. 그룹핑/리스트 모으기: defaultdict(list)
   키가 없으면 빈 리스트[]로 초기화
   추후 알고리즘 문제(그래프, 분류)에서 매우 강력하게 활용될 예정

기존 딕셔너리를 잘 다뤄야 defaultdict도 잘 다룰 수 있음
주의사항
defaultdict는 조회만 해도 키가 생성됨
예: print(my_dict['missing_key'])를 하면 0이 출력되면서 키가 추가됨
따라서 "단순히 키가 있는지 확인할 때"는 주의해서 사용해야 함
.setdefault()와 다른 점?
"setdefault는 매번 호출할 때마다 기본값을 넣어줘야 하지만, defaultdict는 객체를 생성할 때 한 번만 설정하면 되어 반복문 안에서 훨씬 코드가 깔끔해집니다."
**세트**
만날 일 별로 없음..
고유한 항목들의 정렬되지 않은 컬렉션
Set은 내부적으로 해시 테이블을 사용하여 데이터를 저장합니다.
이로 인해 항목의 고유성을 효율적으로 보장하며, 항목의 추가, 삭제, 존재 여부 확인(in 연산)이 데이터의 크기에 관계없이 매우 빠릅니다. 
세트 메서드 
*.add(x)*
세트에 x를 추가 
.update(iterable)
세트에 다른 iterable 요소를 추가 
.clear()
세트의 모든 항목을 제거
*.remove(x)*
세트에서 항목 x를 제거, 항목x가 없을 경우 keyError
.pop()
세트에서 임의의 요소를 제거하고 반환
.discard(x)
세트s에서 항목x를 제거. remove와 달리 에러 없음
에러는 좋은 것이다(!)
문제가 있다는 걸 인지하기 위해(디버깅) remove를 사용하는 것/ 예외처리
잘못돼도 문제가 없을 때 사용(사소해)
*세트의 집합 메서드*
set1.difference(set2)
연산자 주로 사용?
**해시 테이블**
해시 테이블은 '키(Key)'와 '값(Value)'을 짝지어 저장하는 자료구조입니다.
해시 테이블의 원리
1. 키를 해시 함수를 통해 해시 값으로 변환
2. 변환된 해시 값을 인덱스로 삼아 데이터를 저장하거나 찾음
3. 이로 인해 검색, 삽입, 삭제를 매우 빠르게 수행
*해시*
임의의 크기를 가진 데이터를 고정된 크기의 고유한 값으로 변환하는 것
생성된 해시 값(고유한 정수)은 해당 데이터를 식별하는 '지문'역할을 함
*해시 함수*
임의 길이 데이터를 입력 받아 고정 길이 (정수)로 변화해 주는 함수. 이 '정수'가 바로 해시 값
set의 요소 & dict의 키와 해시 테이블 관계 
set
각 요소를 해시 함수로 변환해 나온 해시 값에 맞춰 해시 테이블 내부 버킷(bucket)에 위치시킴
그래서 "순서"라기보다 "버킷 위치(인덱스)"가 요소의 위치를 결정
따라서 set는 순서를 보장하지 않음
dict
키(key) -> 해시 함수 -> 해시 값 -> 해시 테이블에 저장
단 set와 달리 "삽입 순서"는 유지한다는 것이 언어 사양에 따라 보장 됨(python 3.7 이상)
즉, 키를 추가한 순서대로 반복문 순회할 때 나오게 됨
사용자에게 보여지는 키 순서는 삽입 순서가 유지되도록 설계된 것
임의의 수 set()를 pop했을 때 뭐가 나올지 보장x
set의 pop메서드 예시-문자열 
문자열은 해시 계산 시 파이썬의 해시 난수화(Hash Randomization)가 적용되므로 , 실행마다 순서가 달라질 수 있음
*파이썬에서의 해시 함수*
정수
같은 정수는 항상 같은 해시 값을 가짐
예- hash(1)은 여러번 호출해도 결과가 동일
문자열
보안상 이유로 해시 난수화 도입
해시 난수화와 난수 시드
파이썬 프로세스가 새로 시작될 때마다 해시를 계산할 때 사용하는 난수 시드가 달라짐
해시 함수가 매번 바뀌는 것이 아니라, 해시 계산에 쓰이는 시드 값이 실행마다 달라지는 것
이로 인해 동일한 데이터라도 매번 해시 값이 달라져 결과적으로 버킷 배치가 달라짐
*set의 요소 & dict의 키와 해시테이블 관계*
set의 pop()은 "임의의 요소"를 제거하고 반환함
실행할 때마다 다른 요소를 얻는다는 의미에서의 "무작위"가 아니라 "임의"라는 의미에서의 "무작위" (By "arbitrary" the docs dont't mean "random")
내부적으로 해시 테이블(버킷)을 참조하기 때문에, 실행 때마다 다른 요소가 먼저 나올 수 있음 
해시 난수화로 인해 문자열 같은 해시 값이 실행마다 달라질 수 있고, 따라서 set 내부 요소의 배치가 달라질 수 있음
*hashable*
hash() 함수에 넣어 해시 값을 구할 수 있는 객체를 의미 
대부분의 불변 타입은 해시 가능
ex int, float, str, tuple(단 내부에 불변만 있을 경우)
가변형 객체 ex list, dict, set는 기본적으로 해시 불가능
이유: 값이 변하면 해시 값도 달라질 수 있어 해시 테이블 무결성이 깨짐
hashable과 불변성 간의 관계
해시 테이블 ex set, dict의 KEY에는 hashable(해시가 가능한 객체)만 저장 가능
불변 객체는 생성 후 값 변겨이 불가능하므로, 항상 같은 해시 값을 유지
다만, "hash 가능하다 != 불변이다" 가 절대적이지는 않지만
일반적으로 내장 자료형 기준에서는 불변이어야 해시 가능
hashable 객체가 필요한 이유
1. 해시 테이블 기반 자료 구조 사용
set의 요소, dict의 키
중복 방지 & 빠른 검색, 조회
2. 불변성을 통한 일관된 해시 값
한 번 해시 값이 정해지면 바뀌지 않아야 해시 테이블 무결성이 유지
3. 안정성과 예측 가능성 유지
동일한 데이터는 항상 동일한 해시 값을 반환 -> 로직을 단순화


## 새롭게 배운 점
(AI 특강)
-프롬프트의 목적:할루시네이션 줄이기 
LLM-확률기반 추론 범용->페르소나 넣을 시 범위가 좁아짐
zero-shot prompting 예시 없이 지시만
Few-shot Prompting 예시로 패턴을 보여줌 
Chain-of-Thought(단계적 사고를 유도하는 구조)
LLM단독 사용 (Chat 형태)의 한계
하나의 세션 안에서 요구사항 정리, 구현, 검증이 섞임
이전 맥락이 다음 판단에 무의식적으로 영향을 줌
에이전트 개념의 등장 배경
하나의 AI에게 여러 종류의 일을 동시에 맡기면 실패?
Reason: 지금 상황에서 무엇을 해야 할지 판단
Act 선택한 행동 실행
Observe 실행 결과를 확인
이 결과를 다시 Reason 단계로 되돌림
LangChain
에이전트는 목표 달성을 위해 과도하게 넓은 범위에 대한 수정
-일부는 불필요하거나 위험한 변경이 포함됨
-코드 리뷰와 롤백 비용 증
AI를 어시스턴트로서
일을 어떻게 나누는지
역할과 책임을 어떻게 분리하는지
결과를 어떻게 검증하고 되돌릴 수 있는지

"AI를 통제하는 개발자 관점"
chatbot
사용자질문
시스템 
지시사항 
v
agent
사용자화
시드??
tool
RAG
MCP
## 실습 코드

```bash
```

## 문제와 해결 방법

-

## 참고 자료

-

## 내일 공부할 내용

-