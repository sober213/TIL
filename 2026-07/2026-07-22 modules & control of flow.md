# 2026-07-22

## 오늘 공부한 내용

-math 내장 모듈
dot 연산
from 절 사용- 필요한 것만 콕 집어서 가져오는 방식
import 모듈- 모듈 이름이 짧고, 어디서 온 함수인지 드러내고 싶을 때
from 모듈 import 대상 -특정 함수/변수를 자주 반복호출할 때, 모듈 경로가 깊을 때
'as'키워드 (alias)별칭을 부여
* <- 사용하지 않는 편이 좋다 
**패키지(Package)**
패키지는 이미 누군가 잘 만들어둔 코드 쿠러미
가구를 *조립할 때 공구 세트가 있으면 편한* 것처럼, 파이썬도 사용자가 모든 기능을
다 만들기 어렵습니다.  
패키지 사용 목적 
모듈들의 이름공간을 구분하여 충돌을 방지/ 모듈들을 관리
**라이브러리**
라이브러리>패키지>모듈
도서관>책장>책 1권
파이썬 표준 라이브러리 (PSL)
파이썬 언어와 함꼐 제공되는 ??
pip - 외부 패키지들을 설치하도록 도움
패키지 설치 
최신 버전// 특정 버전/ 최소 버전을 명시하여 설치할 수 있음
$ pip install SomePackage
$ pip install SomePackage ==?
???

requests 외부 패키지 설치 및 사용 예시
requests 패키지 
파이썬에서 웹에 요청을 보내고 응답을 받는 걸 아주 쉽게 만들어주는 외부 패키지

**제어문**
constrol statement
코드의 실행 흐름을 제어하는 데 사용되는 구문
조건에 따라 코드 블록을 실행하거나 반복적으로 코드를 실행
*조건문* 
if, elif, else
*반복문*
for 반복
*while 반복*
주어진 조건식을 평가하여 해당 조건이 참인 경우에만 코드 블록을 실행하거나 건너뜀
조건문의 기본 구조
if -조건문의 기본 형태 if문에 작성된 조건을 만족할 때 내부 코드 실행
작성되는 조건은 표현식으로 작성
elif -이전의  조건을 만족하지 못하고 추가로 다른 조건이 필요할 때 사용
여러 개 사용 가능, 순서대로 조건을 봄
else 모든 조건을 만족하지 않으면 실행할 코드
*조건문의 순서*
조건식을 동시에 검사하는 게 아니라 순차적으로 비교
조건식의 순서에 따라 원하는 결과가 나오지 않을 수 있음
*독립 if 여러 개 vs elif*
같아 보이나 결과가 완전히 다릅니다

## 새롭게 배운 점

-

## 실습 코드

```bash
```

## 문제와 해결 방법

-

## 참고 자료

-

## 내일 공부할 내용

-