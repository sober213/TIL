# 2026-07-20

## 오늘 공부한 내용

-할당문(Assignment Statement)
그릇? 
변수명 규칙
영문 알파벳, 언더스코어(_),숫자로 구성
숫자로 시작할 수 없음
대소문자를 구분
아래 키워드는 파이썬의 내부 예약어로 사용할 수 없음
변수, 값 그리고 메모리
고유한 ID (메모리 주소)
타입(Type)
제품의 종류(예: '정수', '실수', '문자열' 등)
**객체** 
**변수는 특정 개체를 '가리키는 (refer/point to)이름표'** 
변수는 담는 게 아니다 
**변수 Variable** 
값을 **나중에 다시 사용**하기 위해 
2. 왼쪽 변수명 확인

재할당
재할당은 이 변수가 가리키는 대상을 새로운 값으로 변경하는 행위
number = 10
개별적으로 

데이터 분류 타입 
Numeric Types/ int, float, complex
Text Sequence Type/ str
Sequence Types/ 

정수 자료형

문자열 
인덱스 예시 
slicing my_str[2:4]
(start생략)- 처음부터 3번 앞까지 
my_ster[:3]
my_str[::-1] #olleh
문자열 변경 
decimal
표현식과 문장
문장(Statement)
특정 '동작(action)'을 지시하는 실행 가능한 코드의 최소 단위
할당문, 조건문, 반복문...
표현식 vs 문장이 헷갈린다면
이 코드를 실행하면 그래서 하나의 값이 남나요
표현식: 네 
어떻게든 계산되거나 평가되어 하나의 값으로 귀결됨
문장: 아니오
컴퓨터에게 특정 동작을 지시하고 끝남
Style Guide
코드의 일관성과 가독성을 향상시키기 위한 규칙과 권장 사항들의 모임 pep8
파이썬에서의 대표적인 Style Guide
ctrl + / 주석처리

리스트 표현 
대괄호[]안에 값들을 쉼표로 구분하여 만듦
숫자, 문자열, 심지어 다른 리스트까지 모든 종류의 데이터를 담을 수 있음
리스트의 시퀀스 특징 
리스트는 시퀀스이므로, 문자열처럼 인덱싱, 슬라이싱, 길이 확인, 반복 등

중첩 리스트(Nested List)
접근하기 - 인덱스를 연달아 사용하여 안쪽 리스트의 값에 접근할 수 있음
1. 먼저 바깥 리스트의 인덱스로 안쪽 리스트를 선택
2. 선택된 안쪽 리스트에 다시 한번 인덱스를 사용
   
리스트
여러 개의... 인덱싱으로 값 수정하기
슬라이싱으로 여러 값 한번에 바꾸기

튜플tuple
여러 개의 값을 순서대로 저장하는 변경 불가능한 시퀀스 자료형
소괄호()안의 값들을 쉼표(,)로 구분하여 만듦
모든 종류의 데이터를 담을 수 있음
리스트와 거의 모든 면에서 비슷하지만, 한번 만들어지면 절대 수정할 수 없다
단일 요소 튜플을 만들 때는 반드시 trailing comma (후행 쉼표)를 사용해야 합니다.
시퀀스로서의 튜플 -공통기능 사용 가능
튜플의 불변성
assignment 할당
튜플은 어디에? (불변 자료형인 이유)
튜플의 불변 특성을 사용하여 내부 동작과 안전한 데이터 전달에 사용됨
거의 쓰지 않음
range 연속된 정수 시퀀스를 형성하는, 변경 불가능한 자료형
주로 반복문과..
매개변수와 인자?
range는 list로 형변환 시 내부 값을 확인 할 수 있습니다.
형변환은 이어지는 ..자세히 다룸
print(my_range)
range의 규칙
step이 양수일 때 기본값:1
숫자가 start부터 stop을 향해 증가
step이 음수일 때 
**dict딕셔너리** 
key-value쌍으로 이루어진 순서와 중복이 없는 변경 가능한 자료형
중괄호 안에 값들이 쉼표로 구분
Key(키)값을 식별하기 위한 고유한 '이름표'
각 값에는 순서가 없음 (인덱스가 없음 -키로 접근)
Key의 규칙 
고유해야 함 Key는 중복될 수 없음
변경 불가능한(immutable)자료형만 사용 가능
O(가능):str, int, float, tuple
x(불가능): list, dict
Value의 규칙
어떤 자료형이든 자유롭게 사용할 수 있음
딕셔너리 값 추가 및 변경
*세트* set 순서와 중복이 없는 변경 가능한 자료형
세트 표현 중괄호 안에 값들을 쉼표(,)로 구분하여 만듦
수학에서의 **집합**과 동일한 연산 처리 가능
두 데이터 그룹 간의 관계를 파악하는 데 매우 효과적
**None** 파이썬에서 '값이 없음'을 표현하는 특별한 데이터 타입
my_variable = None
**Boolean**
'참'(True)과 '거짓(False)' 단 두 가지 값만 가지는 데이터 타입
주로 조건/ 반복문과 함께 사용할 예정
**Collection** 
여러 개의 값을 하나로 묶어 관리하는 자료형들을 통칭
여러 물건을 담는 보관함과 같으며, 파이썬은 목적에 따라 다양한 종류의 컬렉션을 제공합니다. str, list, tuple, range, set, dick 데이터 타입이 모두 Collection 
**불변 vs 가변** 
컬렉션 타입은 생성 후 내용을 변경할 수 있는지 없는지에 따라 '불변'과 '가변' 두 그룹으로 나뉨
**형변환**Type Conversion 
한 데이터 타입을 다른 데이터 타입으로 변화하는 과정 
암시적 형변환 2 +3.14 파이썬이 연산 중에 자동으로 데이터 타입을 변환하는 것
ex- 정수와 실수의 연산에서 정수가 실수로
명시적 형변환 
개발자가 변환하고 싶은 타입을 직접 함수로 지정하여 변환하는 것
str -> int 형식에 맞는 숫자만 가능
int -> str 모두 가능
산술 연산자 
수학적 계산을 위해 사용되는 연산자
복합 연산자
연산과 할당이 함께 이뤄짐 but 가독성, 직관성 떨어짐
짧은 코드가 좋다? no 길더라도 누가 봐도 이해할 수 있는 코드 yes
**비교 연산자** 
! = 부정
== 연산자 
값이 같은지를 비교 
동등성(equality)
예를 들어, 1==True의 경우 파이썬이 내부적으로 True를 1로 간주할 수 있으므로 
True 결과가 나옴
is 연산자 
객체 자체가 같은지를 비교
식별성
두 변수가 완전히 동일한 객체를 가리키는지, 즉 메모리 주소가 같은지를 확인할 때 사용
실제로는 == 가 자주 쓰이는 편
is 대신 ==를 사용해야 하는 이유 
결론: is는 '정체성'을, ==는 '가치'를 
is 연산자는 언제 사용하는가?
주로 싱글턴 객체를 비교할 때 사용함
싱글턴 (Singleton) 객체란?
특정 값에 대해 파이썬 전체에서 단 하나의 객체만 생성되어 재사용되는 특별한 객체
여러 변수가 이 값을 가지더라도, 모두 미리 만들어진 하나의 객체를 함께 가리키게 되므로 항상 같은 메모리 주소를 가짐
파이썬의 대표적인 싱글턴 객체: None, True, False
특정한 키워드에 너무 매몰되지 말 것
추가 예시: 리스트나 객체 비교 시 주의사항
리스트 또는 다른 가변 객체를 비교할 때 값 자체가 같은 지 확인하려면 ==를 사용
**논리 연산자**
and 논리곱 or 논리합 not 논리부정
단축 평가 -논리 연산에서 두 번째 피연산자를 평가하지 않고 결과를 결정하는 동작
파이썬의 '참(True)'과 '거짓(False)'..
단축 평가를 하는 이유 
코드 실행을 최적화하고, 불필요한 연산을 피할 수 있도록 함
**시퀀스 연산자** 
특정 값이 시퀀스나 다른 컬렉션 안에 포함되어 있는지 확인하는 연산자
**시퀀스형 연산자** 
시퀀스 자료형
연산자 우선순위 정리 ()> [], **, ...
Trailing Comma(후행쉼표) 컬렉션 마지막..
기본 규칙 각 요소를 별도의 줄에 작성/ 마지막 요소 뒤에 
좋은 예시
items = [
  'item1'
  'item2'
]
가독성 향상

Q.
1. 현재 로컬 리포지터리 저장소명 확인?
2. clone 한 위치에서 원격 저장소에 push하고 싶을 때 해당 위치에서 로컬 리포지터리 생성?
3. 마크다운 작성할 때 팁?
4. VScode에서 현재 터미널상 디렉토리 파일을 좌측 익스플로러에 표시하는 법? 
5. 문자열 거꾸로 출력? 

A.
1. git rev-parse --show-toplevel 입력결과 맨 뒤 
2. 이미 생성됨. 해당 위치에서 연결된 원격 저장소 확인, push
3. sample.md에서 설명
4. 터미널상 위치 링크 ctrl+클릭
5. 



## 새롭게 배운 점


-유연한 사고 (파고들지 말기)
ex- 유한 정밀도의 경우 
이게 뭔지 확실히 이해하겠다 (x)
오차가 있으니 조심해야겠구나 (o)

1. 코드가 아니라 주석을 먼저 작성
2. 코드 결과를 상상하지 마세요 (한 번에 맞추려고 하지 말 것) 될까?가 아닌 계속 바꾸면서 실행해보기 (print)
값을 누적해서 계속 확인

## 실습 코드

```bash
```

## 문제와 해결 방법

-

## 참고 자료

-

## 내일 공부할 내용

-