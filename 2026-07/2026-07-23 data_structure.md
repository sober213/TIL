# 2026-07-23

## 오늘 공부한 내용
-데이터 구조
-메서드

## 새롭게 배운 점
탐색과 검증 메서드
-isalpha()
-isupper()
-islower()
문자열 조작 메서드
-중요
str.replaced(old, new[,count])
str.strip([chars])
str.split(sep=None, maxsplit=-1)
(정수형태면 map이랑 같이 씀?)
str.join(iterable)
(iterable의 문자열을 연결한 문자열을 반환)
**리스트 값 추가 및 삭제 메서드**
L.appennd(x) 
L.extend(iterable)
L.pop() -리스트 가장 오른쪽에 있는 항목(마지막)을 반환 후 제거 <-> .append()
L.pop(i) -리스트의 인덱스 i에 있는 항목을 반환 후 제거
**얕은 복사**
객체의 최상위 요소만 새로운 메모리에 복사하는 방법 내부에 중첩된 객체가 있다면 그 객체의 참조만 복사
**깊은 복사**
객체의 모든 수준의 요소를 새로운 메모리에 복사하는 방법 중첩된 객체까지 모두 새로운 객체로 생성됨 
**List Comprehentsion**
간결하고 효율적인 리스트 생성 방법
표현식 for 변수 in 시퀀스 
but Comprehension을 남용하지 말자. 이해할 정도면 충분
list를 만드는 3가지 방법 - 적절히 사용하자 
**메서드 체이닝**
여러 메서드 연속 호출하는 방식
메서드 체이닝 주의사항 - 모든 메서드가 체이닝을 지원하는 건 아님 
문자 유형 판별 메서드 - 문자 유형 판별 메서드 isdecimal() igdigit() isnumeric()
파이썬 문법 규격 
BNF 프로그래밍 언어의 문법을 표현하기 위한 표기법
[] 선택적 요소 {} 0번 이상 반복 () 그룹화 
## 실습 코드

```bash
```

## 문제와 해결 방법

-

## 참고 자료

-

## 내일 공부할 내용

-