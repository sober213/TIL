## 백지복습

### Data_Structure_1 

#### 1 자료구조
컴퓨터에서 데이터를 다루는 형식- 선형, 비선형 등 자료를 잘 다루기 위한 형태
불변형, 가변형 등 나뉨 주소 참조하는 방식이 다르다
#### 2 메서드
특정 자료형 클래스에 포함된 내장함수 비스무리한 도구 
자료형에 맞게 써야 한다
공통 .index(), .find()
#### 3 문자열
str 시퀀스, 불변 자료형 - 메서드는 .iscapitalize(앞부분만 대문자로 변경),
.istitle(띄어쓰기 기준 맨 앞부분 변경), .replace(l, m, 2)(old를 new로 2번 바꾼 새로운 문자열 반환), .isupper()(다 대문자인가?), islower?
#### 4 리스트
list: 가변 자료형 - 메서드는 ' '.join(iterable)(공백 기준으로 반복자료형 붙여서 반환), list.append()(한 요소로 취급해 리스트에 추가), .insert(i, x)(i인덱스에 요소 추가), list.pop(i)(i인덱스 요소 삭제하고 반환, 없으면 마지막 요소 삭제) 
#### 4-1 리스트 메서드
remove(x) 맨 앞 요소 삭제, .sort() 정렬만 해줌, sorted()정렬 리스트 반환해주는 내장함수, reverse()? 정렬x 순서만 바꿈
#### 5 메서드 체이닝
여러 메서드를 이어서 쓰는 거 ex- str.replace(l, m).swapcase()
메서드로 반환값이 없는데 거기다 메서드 갈겼다 오류나니 결과를 제대로 숙지 필요